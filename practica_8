#include <stdio.h>
#include <stdlib.h>

/*Escribir un programa que muestre por pantalla todos los múltiplos de 3 entre 1 y 100.*/

int main()
{
    for (int i=1; i<=100; i++){//termina en 100 y va de uno en uno
        if (i%3==0){//divido entre 3 es cero
            printf("%d\n",i);
        }
    }
}
